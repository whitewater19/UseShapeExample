<?xml version="1.0" encoding="utf-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle"> //指定形狀類型，默認為rectangle

    <solid android:color="@color/yellow" /> //指定形狀的填充色


    <padding
        android:bottom="12dp"
        android:left="12dp"
        android:right="12dp"
        android:top="12dp" /> //設定內容區域離邊界的間距

    <corners android:radius="200dp" /> //設定圓角，只適用於 rectangle(越大越圓)

    <stroke
        android:width="2dp"   />//粗細
        android:color="@android:color/darker_gray"
        android:dashGap="4dp"  //距離
        android:dashWidth="4dp" /> //設定描邊
</shape>